package board.service;

import board.dao.BoardDAO;

public class BoardLogoutService implements Food {
	@Override
	public void execute() {
		BoardDAO boardDAO = BoardDAO.getInstance();
		boardDAO.logout();
	}
}
